/**
 * Generated Supabase types live here.
 *
 * After the schema migration is applied to your project, regenerate:
 *   pnpm db:types
 * which runs `supabase gen types typescript ...` into this file. Then type the
 * clients (createBrowserClient<Database>(...) / createServerClient<Database>(...))
 * and your queries become end-to-end typed.
 *
 * Until then this is a permissive placeholder so the app compiles.
 */
export type Database = Record<string, never>;
